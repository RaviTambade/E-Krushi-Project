
spring:
  datasource:
    password: Password
    url: jdbc:mysql://localhost:3306/act?useSSL=false&allowPublicKeyRetrieval=true
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true

import { OrderDetailsAddModel } from './order-details-add-model';

describe('OrderDetailsAddModel', () => {
  it('should create an instance', () => {
    expect(new OrderDetailsAddModel()).toBeTruthy();
  });
});
